'''
Crearemos una clase Punto que representará un punto en un plano 2D. Esta clase utilizará varias funciones mágicas para permitir operaciones matemáticas y comparaciones entre puntos.

Crea una clase llamada Punto que tenga dos atributos:
  x: Coordenada x del punto.
  y: Coordenada y del punto.

Implementa el método __init__ para inicializar las coordenadas x y y del punto.

Implementa la función __str__ para que al imprimir un objeto de tipo Punto, se muestre en el formato (x, y).

Implementa la función __add__ para que puedas sumar dos objetos Punto utilizando el operador +. La suma de dos puntos debe devolver un nuevo punto cuyas coordenadas sean la suma de las coordenadas correspondientes.

Implementa la función __sub__ para que puedas restar dos objetos Punto utilizando el operador -. La resta de dos puntos debe devolver un nuevo punto cuyas coordenadas sean la resta de las coordenadas correspondientes.

Implementa la función __eq__ para que puedas comparar dos objetos Punto utilizando el operador ==. Dos puntos se consideran iguales si sus coordenadas x e y son iguales.

Implementa la función __lt__ para que puedas comparar si un punto es menor que otro utilizando el operador <. Un punto se considera menor que otro si la suma de sus coordenadas es menor que la suma de las coordenadas del otro punto.

Para probar que la implementación es correcta, crea varios objetos Punto y prueba las operaciones de suma, resta, comparación de igualdad, y comparación de orden.
'''
class Punto:
  pass